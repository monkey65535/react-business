import Axios from 'axios';
import {message} from 'antd';
import Qs from 'qs';


//reducer
const initState = {
    cartProductVoList: []
};

export function orderInfo(state = initState, action) {
    switch (action.type) {
        default:
            return state
    }
}